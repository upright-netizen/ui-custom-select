{
  "name": "ui-custom-select",
  "version": "0.0.0",
  "description": "The idea here is to have some html/css/js that looks and acts like a select dropdown, and behind the scenes uses a real select. You know, progressive enhancement and all that jazz",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nathan Stilwell",
  "license": "ISC",
  "devDependencies": {
    "grunt-contrib-connect": "^0.8.0",
    "grunt-contrib-jshint": "^0.10.0",
    "grunt": "^0.4.5",
    "grunt-contrib-watch": "^0.6.1"
  }
}
